/** Application Reducer */

const DEFAULT_STATE = {
    query: '',
    anagrams: []
}

export default (state=DEFAULT_STATE, action) => {
    switch(action.type){
        case 'UPDATE_QUERY':
        default:
            return state
    }
}